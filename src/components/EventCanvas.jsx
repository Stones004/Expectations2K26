import {useEffect} from 'react';
import {Link} from 'react-router-dom';
export default function EventCanvas({event,onClose}){useEffect(()=>{const key=e=>e.key==='Escape'&&onClose();const previous=document.body.style.overflow;document.body.style.overflow='hidden';addEventListener('keydown',key);return()=>{document.body.style.overflow=previous;removeEventListener('keydown',key)}},[onClose]);if(!event)return null;return <div className="event-canvas-backdrop" role="presentation" onMouseDown={onClose}><section className="event-canvas event-canvas-brief" role="dialog" aria-modal="true" aria-labelledby="eventCanvasTitle" onMouseDown={e=>e.stopPropagation()} style={{'--event-image':`url(${event.image})`}}><div className="canvas-aurora" aria-hidden="true"><i/><i/><i/></div><div className="canvas-shimmer" aria-hidden="true"/><button className="canvas-close" type="button" onClick={onClose} aria-label="Close event canvas">×</button><div className="canvas-brief-content"><h2 id="eventCanvasTitle">{event.title}</h2><p>{event.summary}</p><Link className="canvas-route" to={`/events/${event.slug}`}>View full event&nbsp; ↗</Link></div></section></div>}
