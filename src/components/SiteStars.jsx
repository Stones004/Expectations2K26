import {useEffect,useRef} from 'react';
export default function SiteStars(){const ref=useRef(null);useEffect(()=>{const c=ref.current,ctx=c.getContext('2d'),dpr=Math.min(devicePixelRatio||1,1.75);let w,h,raf,scrollY=0;const mouse={x:-9999,y:-9999,tx:-9999,ty:-9999};const stars=Array.from({length:96},(_,i)=>({x:Math.random(),y:Math.random(),r:.5+Math.random()*1.45,p:Math.random()*7,tw:i%8===0}));const resize=()=>{w=c.width=innerWidth*dpr;h=c.height=innerHeight*dpr};const move=e=>{mouse.tx=e.clientX*dpr;mouse.ty=e.clientY*dpr};resize();addEventListener('resize',resize);addEventListener('mousemove',move,{passive:true});addEventListener('scroll',()=>scrollY=window.scrollY,{passive:true});let t=0;const draw=()=>{t+=.016;mouse.x+=(mouse.tx-mouse.x)*.09;mouse.y+=(mouse.ty-mouse.y)*.09;ctx.clearRect(0,0,w,h);let near=[];for(const s of stars){let x=s.x*w+Math.sin(t*.3+s.p)*5*dpr,y=(s.y*h-scrollY*.018*dpr+h)%h,dist=Math.hypot(mouse.x-x,mouse.y-y),n=Math.max(0,1-dist/(205*dpr)),pulse=s.tw?.48+.52*Math.max(0,Math.sin(t*1.9+s.p)):.68,r=s.r*(1+n*1.55)*dpr;near.push({x,y,n});ctx.fillStyle=`rgba(235,225,196,${Math.min(.13+pulse*.28+n*.45,.95)})`;ctx.beginPath();ctx.arc(x,y,r,0,7);ctx.fill();if((s.tw&&pulse>.9)||n>.78){ctx.strokeStyle=`rgba(255,244,210,${.3+n*.5})`;ctx.lineWidth=.5*dpr;ctx.beginPath();ctx.moveTo(x-4*dpr,y);ctx.lineTo(x+4*dpr,y);ctx.moveTo(x,y-4*dpr);ctx.lineTo(x,y+4*dpr);ctx.stroke()}}for(let i=0;i<near.length;i++)for(let j=i+1;j<near.length;j++){let a=near[i],b=near[j],dist=Math.hypot(a.x-b.x,a.y-b.y);if(a.n>.15&&b.n>.15&&dist<92*dpr){ctx.strokeStyle=`rgba(201,162,75,${.16*Math.min(a.n,b.n)*(1-dist/(92*dpr))})`;ctx.lineWidth=.5*dpr;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}}raf=requestAnimationFrame(draw)};draw();return()=>{cancelAnimationFrame(raf);removeEventListener('resize',resize);removeEventListener('mousemove',move)}},[]);return <canvas ref={ref} className="site-stars" aria-hidden/>}
